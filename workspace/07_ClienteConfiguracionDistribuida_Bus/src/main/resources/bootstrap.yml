server:
  port: 8880


endpoints:
  refresh:
    sensitive: false

spring:
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:9080
  application:
    name: holamundo
  rabbitmq:
    host: localhost
    port: 5672